// In main.js, import the Widget class from widget.js and create a new instance of it
import { Widget } from "./Widget.js";

let widget = new Widget();

// Append the widget element to the document body
document.body.appendChild(widget.element);
